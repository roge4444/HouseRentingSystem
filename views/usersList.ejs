<!DOCTYPE html>
<html>
  <head>
  	<meta charset-"utf8">
	<title></title>
    
   
  </head>
  <body>
  	<a href="/">主頁</a>
  <% for(var i=0; i<users.length; i++) { %>
    <% if(users[i].rental >= 1){ %>
       <p> -----------------------------------</p>
       <p> Your name is <%= users[i].username %></p>   
		   <p>	Your 租金 is <%= users[i].rental %>  </p>
		   <p>	Your 坪數 is <%= users[i].size %>   </p>
       <p> -----------------------------------</p>
    <% } %>
       
    <% } %>
  </body>
</html>

